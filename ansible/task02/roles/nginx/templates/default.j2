server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root {{ source_dir }};
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
